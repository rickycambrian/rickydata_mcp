name: Secret Guard

on:
  push:
    branches: [main, staging]
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 2

    - name: Check for secrets in diff
      run: node scripts/secret-guard.mjs --range HEAD~1..HEAD
